interface:
  display_name: "Multi-Agent Orchestrator"
  short_description: "Checklist-first master/subagent workflow"
  default_prompt: "Use $multi-agent-orchestration only when the user explicitly requests subagents, parallel agents, or a master/worker workflow."
